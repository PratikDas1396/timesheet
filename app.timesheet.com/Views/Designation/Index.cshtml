@model app.timesheet.com.DesignationViewModel
@{
    /**/

    ViewBag.Title = "Designation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12 d-flex" id="divList">
                @{
                    Html.RenderPartial("_List", Model.DesignationList);
                }
            </div>
        </div>
    </div>


</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            
            $(document).on("click", ".global-modal-close", function () {
                const view = "@Url.Content("~/Designation/List")";
                $("#divList").load(view)
            })
        });
    </script>
}
